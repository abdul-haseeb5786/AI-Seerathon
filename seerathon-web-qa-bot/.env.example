# Corpus API base, exactly as given in the role brief.
SEERATHON_CORPUS_BASE=https://api.islamicdesk.com/api/seerathon/corpus

# The brief does not document an auth scheme for this API. If it turns out to
# need a key, set it here AND update the header name in lib/corpus.ts
# (currently assumes `Authorization: Bearer <key>` — could be `x-api-key`,
# a query param, etc. Confirm with the organizers.)
SEERATHON_API_KEY=
